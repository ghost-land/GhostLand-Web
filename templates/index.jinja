<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/index.css') }}">
    <title>Ghost Land | .at</title>
</head>
<body>
    {% include 'components/header.html' %}

    <main>
        <div class="latest-announcements">
            <h2>Latest Announcements</h2>
            <div class="announcements-previews">
                <!-- TODO: remove examples -->
                <ul>
                    <li>
                        Nlib public release scheduled for Q3 202
                    </li>
                    <li>
                        Ghost eShop now supports over 15,000 game files
                    </li>
                    <li>
                        New partnership program for indie developers coming soon
                    </li>
                </ul>
            </div>
        </div>

        <div class="innovating">
            <p>Innovating since...</p>
            <h1 class="year">2020</h1>
            <p>
                Ghost Land is at the forefront of technological innovation, creating  solutions that transform how users interact with games and information.  Our dedicated team of experts is committed to pushing boundaries and  delivering cutting-edge projects.
            </p>
        </div>

        <div class="projects category">
            <h1 class="our-projects">
                Our Projects
            </h1>
            <div class="project-cards">
                {% for project in settings.get('our-projects', []) %}
                    <div class="project-card">
                        <img src="{{ project.get('image') }}">
                        <h3>{{ project.get('title', 'PROJECT_TITLE') }}</h3>
                        <p>{{ project.get('description', 'PROJECT_DESCRIPTION') }}</p>
                        <a href="{{ project.get('button', {}).get('url', '#') }}">
                            <button>
                                {{ project.get('button', {}).get('text', 'Coming soon...') }}
                            </button>
                        </a>
                    </div>
                {% endfor %}
            </div>
        </div>

        <div class="contact category">
            <h1 class="contact">
                You like us? <span class=red>Connect with us.<span>
            </h1>
            <p>Bring your project to life or explore collaboration opportunities.</p>
            <a href="mailto:pro@ghostland.at">
                {{ open('static/images/mail.svg').read() }}
                pro@ghostland.at
            </a>
        </div>

        <div class="follow-us category">
            <h1 class="follow-us">
                Follow our <span class=red>Journey<span>
            </h1>
            <div class="socials">
                <a href="{{settings['socials'].get('twitter')}}" target="_blank" class="circle">{{ open('static/images/twitter.svg').read() }}</a>
                <a href="{{settings['socials'].get('discord')}}" target="_blank" class="circle">{{ open('static/images/discord.svg').read() }}</a>
                <a href="{{settings['socials'].get('telegram')}}" target="_blank" class="circle">{{ open('static/images/telegram.svg').read() }}</a>
            </div>
        </div>
    </main>

    {% include 'components/footer.jinja' %}
</body>
</html>